%(dot(n2,n2)*dot(cross(n1,n4),cross(n1,n4))-dot(n2,n1)*dot(cross(n2,n4),cross(n1,n4))-dot(n2,n4)*dot(cross(n1,n4),cross(n1,n2)))/(dot(n1,cross(n2,n4)))^2

%(dot(n1,n1)*dot(cross(n3,n4),cross(n3,n4))-(dot(n1,n3))^2*dot(n4,n4)+(dot(n1,n4))^2*dot(n3,n3))/(dot(n1,cross(n3,n4)))^2

%invM*vol

%dot(n3,n3)+(dot(n3,cross(n1,n4)))^2/(dot(cross(n1,n4),cross(n1,n4)))

%(dot(n1,n2)*dot(cross(n1,n4),cross(n3,n4))+dot(n2,n4)*dot(cross(n1,n3),cross(n1,n4)))/(dot(cross(n1,n4),cross(n1,n4)))^2

(n12*norm(cross(n3,n4))^2-(n23-dot(n2,cross(n1,n4))*dot(n3,cross(n1,n4))/norm(cross(n1,n4))^2)*dot(cross(n3,n4),cross(n1,n4))+dot(n2,n4)*dot(cross(n3,n4),cross(n1,n3)))/dot(n1,cross(n3,n4))^2

(n12*norm(cross(n2,n4))^2-(1+dot(n2,cross(n1,n4))^2/norm(cross(n1,n4))^2)*dot(cross(n2,n4),cross(n1,n4))+dot(n2,n4)*dot(cross(n2,n4),cross(n1,n2)))/dot(n1,cross(n2,n4))^2

dot(cross(n1,n4),cross(n2,n4))/norm(cross(n1,n4))^2